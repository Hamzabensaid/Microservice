spring:
  profiles:
    active: dev
  servlet:
    multipart:
      max-file-size: 50MB
  docker:
    compose:
      enabled: false
springdoc:
  default-produces-media-type: application/json

server:
  servlet:
    context-path: /api/v1/
